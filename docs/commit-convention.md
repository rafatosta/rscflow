# Commit convention

Use Conventional Commits in English:

`type(scope): description`

Examples: `feat(domain): add project export schema`, `test(rules): cover validated criterion`. Do not commit automatically; maintainers authorize commits explicitly.
